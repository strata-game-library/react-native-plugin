/**
 * Preset animation state machine factories.
 *
 * Provides pre-configured machines for common game animation patterns
 * like locomotion and combat.
 *
 * @module core/animation/state-machine
 */
/**
 * Creates a locomotion animation machine with standard movement states.
 *
 * @param animations - Map of state names to animation clip names
 * @returns Configured animation machine for locomotion
 *
 * @example
 * ```typescript
 * const locomotion = createLocomotionMachine({
 *   idle: 'Character_Idle',
 *   walk: 'Character_Walk',
 *   run: 'Character_Run',
 *   jump: 'Character_Jump'
 * });
 * ```
 */
export declare function createLocomotionMachine(animations: Partial<Record<'idle' | 'walk' | 'run' | 'jump' | 'fall' | 'land', string>>): import("xstate").StateMachine<{
    currentAnimation: string;
    previousAnimation: string | null;
    blendFactor: number;
    playbackSpeed: number;
    timeInState: number;
    isPaused: boolean;
    metadata?: Record<string, unknown>;
}, import("xstate").AnyEventObject, Record<string, import("xstate").AnyActorRef>, import("xstate").ProvidedActor, import("xstate").ParameterizedObject, import("xstate").ParameterizedObject, string, import("xstate").StateValue, string, unknown, import("xstate").NonReducibleUnknown, import("xstate").EventObject, import("xstate").MetaObject, any>;
/**
 * Creates a combat animation machine with attack and damage states.
 *
 * @param animations - Map of combat state names to animation clips
 * @returns Configured animation machine for combat
 *
 * @example
 * ```typescript
 * const combat = createCombatMachine({
 *   idle: 'Combat_Idle',
 *   attack: 'Attack_Slash',
 *   hit: 'Take_Damage',
 *   death: 'Death'
 * });
 * ```
 */
export declare function createCombatMachine(animations: Partial<Record<'idle' | 'attack' | 'hit' | 'death' | 'dodge' | 'block', string>>): import("xstate").StateMachine<{
    currentAnimation: string;
    previousAnimation: string | null;
    blendFactor: number;
    playbackSpeed: number;
    timeInState: number;
    isPaused: boolean;
    metadata?: Record<string, unknown>;
}, import("xstate").AnyEventObject, Record<string, import("xstate").AnyActorRef>, import("xstate").ProvidedActor, import("xstate").ParameterizedObject, import("xstate").ParameterizedObject, string, import("xstate").StateValue, string, unknown, import("xstate").NonReducibleUnknown, import("xstate").EventObject, import("xstate").MetaObject, any>;
//# sourceMappingURL=presets.d.ts.map