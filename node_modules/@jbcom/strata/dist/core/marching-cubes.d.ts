/**
 * Marching Cubes implementation for generating meshes from SDFs
 *
 * Marching Cubes is an algorithm for extracting a polygonal mesh of an
 * isosurface from a 3D scalar field (like an SDF).
 *
 * Lifted from Otterfall procedural terrain system.
 */
import * as THREE from 'three';
export interface MarchingCubesResult {
    vertices: Float32Array;
    normals: Float32Array;
    indices: Uint32Array;
}
export interface MarchingCubesOptions {
    resolution: number;
    bounds: {
        min: THREE.Vector3;
        max: THREE.Vector3;
    };
    isoLevel?: number;
}
/**
 * Generate a mesh from an SDF using the Marching Cubes algorithm
 */
export declare function marchingCubes(sdf: (p: THREE.Vector3) => number, options: MarchingCubesOptions): MarchingCubesResult;
/**
 * Create a Three.js BufferGeometry from marching cubes result
 */
export declare function createGeometryFromMarchingCubes(result: MarchingCubesResult): THREE.BufferGeometry;
/**
 * Chunk-based terrain generation for large worlds
 */
export interface TerrainChunk {
    geometry: THREE.BufferGeometry;
    boundingBox: THREE.Box3;
    position: THREE.Vector3;
}
export declare function generateTerrainChunk(sdf: (p: THREE.Vector3) => number, chunkPosition: THREE.Vector3, chunkSize: number, resolution: number): TerrainChunk;
//# sourceMappingURL=marching-cubes.d.ts.map