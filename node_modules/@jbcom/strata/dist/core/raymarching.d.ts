/**
 * Ray Marching - Core TypeScript (no React)
 *
 * Pure TypeScript functions for ray marching setup
 */
import * as THREE from 'three';
export interface RaymarchingMaterialOptions {
    sdfFunction: string;
    maxSteps?: number;
    maxDistance?: number;
    minDistance?: number;
    backgroundColor?: THREE.ColorRepresentation;
    fogStrength?: number;
    fogColor?: THREE.ColorRepresentation;
    cameraPosition?: THREE.Vector3;
    cameraMatrix?: THREE.Matrix4;
    resolution?: THREE.Vector2;
    time?: number;
}
/**
 * Create ray marching material (pure TypeScript)
 */
export declare function createRaymarchingMaterial(options: RaymarchingMaterialOptions): THREE.ShaderMaterial;
/**
 * Create fullscreen quad geometry for ray marching (pure TypeScript)
 */
export declare function createRaymarchingGeometry(): THREE.PlaneGeometry;
//# sourceMappingURL=raymarching.d.ts.map