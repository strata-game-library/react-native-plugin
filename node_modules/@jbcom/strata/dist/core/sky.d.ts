/**
 * Sky Materials - Core TypeScript (no React)
 *
 * Pure TypeScript functions for creating sky materials
 */
import * as THREE from 'three';
export interface TimeOfDayState {
    sunIntensity: number;
    sunAngle: number;
    ambientLight: number;
    starVisibility: number;
    fogDensity: number;
}
export interface WeatherState {
    intensity: number;
}
export interface SkyMaterialOptions {
    timeOfDay: Partial<TimeOfDayState>;
    weather?: Partial<WeatherState>;
    gyroTilt?: THREE.Vector2;
    time?: number;
}
/**
 * Create sky material (pure TypeScript)
 */
export declare function createSkyMaterial(options: SkyMaterialOptions): THREE.ShaderMaterial;
/**
 * Create sky geometry (pure TypeScript)
 */
export declare function createSkyGeometry(size?: [number, number]): THREE.PlaneGeometry;
//# sourceMappingURL=sky.d.ts.map