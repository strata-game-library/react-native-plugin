/**
 * Texture loading utilities
 *
 * Lifted from Otterfall terrain material loader.
 */
import * as THREE from 'three';
export type BiomeType = 'marsh' | 'forest' | 'desert' | 'tundra' | 'savanna' | 'mountain' | 'scrubland';
export interface TerrainTextures {
    albedo: THREE.Texture;
    normal: THREE.Texture;
    roughness: THREE.Texture;
    ao: THREE.Texture;
}
/**
 * Load and configure a texture with proper compression settings for mobile
 */
export declare function loadTexture(path: string, renderer: THREE.WebGLRenderer): THREE.Texture;
/**
 * Load all PBR textures for a specific biome
 */
export declare function loadBiomeTextures(biome: BiomeType, renderer: THREE.WebGLRenderer, basePath?: string): TerrainTextures;
/**
 * Create a PBR material from loaded textures
 */
export declare function createTerrainMaterial(textures: TerrainTextures, options?: {
    color?: THREE.Color;
    metalness?: number;
    roughnessScale?: number;
}): THREE.MeshStandardMaterial;
/**
 * Preload textures for multiple biomes to reduce loading time during gameplay
 */
export declare function preloadBiomeTextures(biomes: BiomeType[], renderer: THREE.WebGLRenderer, basePath?: string): Promise<Map<BiomeType, TerrainTextures>>;
/**
 * Clear texture cache to free memory
 */
export declare function clearTextureCache(): void;
/**
 * Standard texture types in PBR workflows
 */
export type StandardTextureType = 'albedo' | 'diffuse' | 'basecolor' | 'normal' | 'roughness' | 'smoothness' | 'metallic' | 'metalness' | 'ao' | 'ambient_occlusion' | 'height' | 'displacement' | 'emissive' | 'emission' | 'opacity' | 'alpha';
/**
 * Common texture provider naming patterns
 */
export declare const TEXTURE_PROVIDER_PATTERNS: {
    ambientCG: {
        albedo: string;
        normal: string;
        roughness: string;
        metallic: string;
        ao: string;
        height: string;
        normalDX: string;
    };
    polyHaven: {
        albedo: string;
        normal: string;
        roughness: string;
        metallic: string;
        ao: string;
        height: string;
        normalDX: string;
    };
    generic: {
        albedo: string;
        normal: string;
        roughness: string;
        metallic: string;
        ao: string;
        height: string;
        emissive: string;
    };
};
/**
 * Load a texture set from a provider with automatic naming detection
 */
export declare function loadTextureSet(basePath: string, materialName: string, renderer: THREE.WebGLRenderer, provider?: keyof typeof TEXTURE_PROVIDER_PATTERNS, extension?: string): Partial<TerrainTextures> & {
    metallic?: THREE.Texture;
    height?: THREE.Texture;
};
//# sourceMappingURL=texture-loader.d.ts.map