/**
 * GPU-Driven Instancing System
 *
 * Uses drei's Instances component for true GPU-driven instancing
 * with wind animation and LOD calculations performed on the GPU.
 *
 * Optimized for mobile, web, and desktop with support for thousands
 * of instances with minimal CPU overhead.
 *
 * Lifted from Otterfall procedural rendering system.
 */
import * as THREE from 'three';
import { type BiomeData, type InstanceData } from '../core/instancing';
export type { BiomeData, InstanceData } from '../core/instancing';
export declare function generateInstanceData(count: number, areaSize: number, heightFunc: (x: number, z: number) => number, biomes?: BiomeData[], allowedBiomes?: string[], seed?: number): InstanceData[];
interface GPUInstancedMeshProps {
    /** The geometry to use for each instance */
    geometry: THREE.BufferGeometry;
    /** The material to use for each instance */
    material: THREE.Material;
    /** Maximum number of instances to render */
    count: number;
    /** Array of instance data (position, rotation, scale) */
    instances: InstanceData[];
    /**
     * Enable wind animation effect
     * @remarks Currently not implemented - reserved for future GPU shader integration
     */
    enableWind?: boolean;
    /**
     * Strength of wind animation (0-1)
     * @remarks Currently not implemented - reserved for future GPU shader integration
     */
    windStrength?: number;
    /**
     * Distance at which LOD transitions occur
     * @remarks Currently not implemented - reserved for future GPU shader integration
     */
    lodDistance?: number;
    /** Enable frustum culling */
    frustumCulled?: boolean;
    /** Enable shadow casting */
    castShadow?: boolean;
    /** Enable shadow receiving */
    receiveShadow?: boolean;
}
export declare function GPUInstancedMesh({ geometry, material, count, instances, enableWind, windStrength, lodDistance, frustumCulled, castShadow, receiveShadow, }: GPUInstancedMeshProps): import("react/jsx-runtime").JSX.Element;
interface VegetationProps {
    count?: number;
    areaSize?: number;
    biomes?: BiomeData[];
    heightFunc?: (x: number, z: number) => number;
    height?: number;
    color?: THREE.ColorRepresentation;
}
/**
 * Instanced grass blades
 */
export declare function GrassInstances({ count, areaSize, biomes, heightFunc, height, color, }: VegetationProps): import("react/jsx-runtime").JSX.Element;
/**
 * Instanced trees
 */
export declare function TreeInstances({ count, areaSize, biomes, heightFunc, }: VegetationProps): import("react/jsx-runtime").JSX.Element;
/**
 * Instanced rocks
 */
export declare function RockInstances({ count, areaSize, biomes, heightFunc, }: VegetationProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Instancing.d.ts.map