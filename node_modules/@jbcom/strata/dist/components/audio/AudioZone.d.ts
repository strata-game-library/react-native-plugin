/**
 * AudioZone Component
 *
 * Spatial audio zone that triggers audio when listener enters.
 * @module components/audio
 */
import type { AudioZoneProps, AudioZoneRef } from './types';
/**
 * Spatial audio zone that triggers audio when listener enters.
 *
 * @example
 * ```tsx
 * <AudioZone
 *   position={[0, 0, -20]}
 *   geometry="sphere"
 *   radius={15}
 *   audioUrl="/sounds/cave-ambience.mp3"
 *   fadeTime={1}
 *   onEnter={() => setEnvironment('cave')}
 *   onExit={() => setEnvironment('outdoor')}
 * />
 * ```
 */
export declare const AudioZone: import("react").ForwardRefExoticComponent<AudioZoneProps & import("react").RefAttributes<AudioZoneRef>>;
//# sourceMappingURL=AudioZone.d.ts.map