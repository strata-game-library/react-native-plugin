/**
 * AudioEmitter Component
 *
 * Positional audio emitter that can follow an object.
 * @module components/audio
 */
import type { AudioEmitterProps, AudioEmitterRef } from './types';
/**
 * Positional audio emitter that can follow an object.
 *
 * @example
 * ```tsx
 * const carRef = useRef<THREE.Group>(null);
 *
 * <group ref={carRef}>
 *   <Car />
 * </group>
 * <AudioEmitter
 *   url="/sounds/engine.mp3"
 *   follow={carRef}
 *   loop={true}
 *   autoplay={true}
 *   refDistance={5}
 * />
 * ```
 */
export declare const AudioEmitter: import("react").ForwardRefExoticComponent<AudioEmitterProps & import("react").RefAttributes<AudioEmitterRef>>;
//# sourceMappingURL=AudioEmitter.d.ts.map