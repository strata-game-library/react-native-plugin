/**
 * Fur Preset - Multi-layer fur rendering system
 *
 * Provides GPU-accelerated fur rendering using layered geometry
 * for characters, animals, and organic surfaces.
 */
import * as THREE from 'three';
export interface FurOptions {
    baseColor?: THREE.ColorRepresentation;
    tipColor?: THREE.ColorRepresentation;
    layerCount?: number;
    spacing?: number;
    windStrength?: number;
    gravityDroop?: number;
}
export interface FurUniforms {
    layerOffset: {
        value: number;
    };
    spacing: {
        value: number;
    };
    colorBase: {
        value: THREE.Color;
    };
    colorTip: {
        value: THREE.Color;
    };
    time: {
        value: number;
    };
}
/**
 * Create fur material for a single shell layer
 */
export declare function createFurMaterial(layerIndex: number, totalLayers: number, options?: FurOptions): THREE.ShaderMaterial;
/**
 * Create complete fur system with all shell layers
 */
export declare function createFurSystem(geometry: THREE.BufferGeometry, baseMaterial: THREE.Material, options?: FurOptions): THREE.Group;
/**
 * Update fur uniforms for animation
 */
export declare function updateFurUniforms(furSystem: THREE.Group, time: number): void;
//# sourceMappingURL=index.d.ts.map