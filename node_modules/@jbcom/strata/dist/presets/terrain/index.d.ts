/**
 * Terrain Preset - SDF-based terrain generation
 *
 * Provides terrain generation using SDFs and marching cubes
 * for creating landscapes, caves, and organic terrain.
 */
import * as THREE from 'three';
import { type TerrainChunk } from '../../core/marching-cubes';
import { type BiomeData as SDFBiomeData } from '../../core/sdf';
export type { MarchingCubesOptions, TerrainChunk } from '../../core/marching-cubes';
export type { BiomeData } from '../../core/sdf';
export interface TerrainOptions {
    biomes: SDFBiomeData[];
    resolution?: number;
    bounds?: {
        min: THREE.Vector3;
        max: THREE.Vector3;
    };
    material?: THREE.Material;
}
/**
 * Create terrain geometry from SDF
 */
export declare function createTerrainGeometry(options: TerrainOptions): THREE.BufferGeometry;
/**
 * Create terrain mesh with material
 */
export declare function createTerrainMesh(options: TerrainOptions): THREE.Mesh;
/**
 * Create chunked terrain for large worlds
 */
export declare function createChunkedTerrain(biomes: SDFBiomeData[], chunkPositions: THREE.Vector3[], chunkSize: number, resolution: number): TerrainChunk[];
/**
 * Get terrain height at a point (for character positioning, etc.)
 */
export declare function getTerrainHeightAt(x: number, z: number, biomes: SDFBiomeData[]): number;
//# sourceMappingURL=index.d.ts.map