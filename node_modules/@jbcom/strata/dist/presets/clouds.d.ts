/**
 * Cloud Presets
 *
 * Pre-configured cloud settings for common weather and atmospheric conditions
 */
import type { CloudLayerConfig, CloudSkyConfig, DayNightConfig, WindConfig } from '../core/clouds';
export type CloudPresetName = 'clear' | 'partlyCloudy' | 'overcast' | 'stormy' | 'sunset';
export interface CloudPreset {
    name: string;
    description: string;
    layers: CloudLayerConfig[];
    wind: WindConfig;
    dayNight: DayNightConfig;
    volumetric?: {
        cloudBase: number;
        cloudHeight: number;
        coverage: number;
        density: number;
    };
}
export declare const cloudPresets: Record<CloudPresetName, CloudPreset>;
export declare function getCloudPreset(name: CloudPresetName): CloudPreset;
export declare function createCloudPresetConfig(name: CloudPresetName): CloudSkyConfig;
export declare function interpolateCloudPresets(from: CloudPresetName, to: CloudPresetName, t: number): CloudPreset;
export declare function getAllCloudPresetNames(): CloudPresetName[];
//# sourceMappingURL=clouds.d.ts.map