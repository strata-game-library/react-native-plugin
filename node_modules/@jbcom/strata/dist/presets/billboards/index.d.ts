/**
 * Billboard Preset - Always-face-camera sprites
 *
 * Provides billboard rendering for trees, grass, UI elements,
 * and other objects that should always face the camera.
 */
import * as THREE from 'three';
export interface BillboardOptions {
    texture: THREE.Texture;
    size?: number | {
        width: number;
        height: number;
    };
    color?: THREE.Color;
    transparent?: boolean;
    opacity?: number;
    alphaTest?: number;
    side?: THREE.Side;
}
/**
 * Create a billboard mesh that always faces the camera
 */
export declare function createBillboard(options: BillboardOptions): THREE.Mesh;
/**
 * Create instanced billboards for performance
 */
export declare function createBillboardInstances(count: number, positions: THREE.Vector3[], options: BillboardOptions): THREE.InstancedMesh;
/**
 * Create sprite sheet animation billboard
 */
export declare function createAnimatedBillboard(texture: THREE.Texture, frameCount: {
    x: number;
    y: number;
}, frameRate?: number, options?: Omit<BillboardOptions, 'texture'>): THREE.Mesh & {
    update: (deltaTime: number) => void;
};
//# sourceMappingURL=index.d.ts.map