/**
 * Reflection Probes Preset - Real-time reflections
 *
 * Provides reflection probes for real-time reflections,
 * environment mapping, and glossy surfaces.
 */
import * as THREE from 'three';
export interface ReflectionProbeOptions {
    position: THREE.Vector3;
    size?: number;
    resolution?: number;
    updateRate?: number;
    boxProjection?: boolean;
    boxSize?: THREE.Vector3;
    renderObjects?: (scene: THREE.Scene) => THREE.Object3D[];
}
export interface ReflectionProbe {
    probe: THREE.CubeTexture;
    camera: THREE.CubeCamera;
    update: (renderer: THREE.WebGLRenderer, scene: THREE.Scene) => void;
    dispose: () => void;
}
/**
 * Create a reflection probe
 */
export declare function createReflectionProbe(options: ReflectionProbeOptions): ReflectionProbe;
/**
 * Create environment map from reflection probe
 */
export declare function createEnvironmentMap(renderer: THREE.WebGLRenderer, scene: THREE.Scene, position: THREE.Vector3, resolution?: number): THREE.CubeTexture;
/**
 * Apply reflection probe to material
 */
export declare function applyReflectionProbe(material: THREE.Material, probe: THREE.CubeTexture, intensity?: number): void;
/**
 * Create box-projected reflection
 */
export declare function createBoxProjectedReflection(material: THREE.Material, probe: THREE.CubeTexture, boxPosition: THREE.Vector3, boxSize: THREE.Vector3): void;
/**
 * Reflection probe manager for multiple probes
 */
export declare class ReflectionProbeManager {
    private probes;
    private renderer;
    private scene;
    constructor(renderer: THREE.WebGLRenderer, scene: THREE.Scene);
    /**
     * Add a reflection probe
     */
    addProbe(name: string, options: ReflectionProbeOptions): ReflectionProbe;
    /**
     * Get a reflection probe
     */
    getProbe(name: string): ReflectionProbe | undefined;
    /**
     * Remove a reflection probe
     */
    removeProbe(name: string): void;
    /**
     * Update all probes
     */
    update(): void;
    /**
     * Dispose all probes
     */
    dispose(): void;
}
//# sourceMappingURL=index.d.ts.map